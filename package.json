{
  "name": "private-messenger-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create private_messenger",
    "db:migrate": "wrangler d1 execute private_messenger --remote --file=./schema.sql",
    "gen:vapid": "node ./scripts/generate-vapid.mjs"
  },
  "dependencies": {
    "nanoid": "^5.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.11.0"
  }
}
